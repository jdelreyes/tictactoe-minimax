#TIC TAC TOE class:
board: char[][]
player: Player
aiSymbol: char

TicTacToe(String playerName, char playerSymbol, char aiSymbol, String aiMode)
    - this should make the game

makeMove(move, symbol)
    - this should make the move regardless of the player

isAvailable(int column, int row)
    - this should check if the spot is available

weakAiMode(move)
    - this should be triggered when the player chooses to play with weak AI

isFull()
    - should check if the board is winning